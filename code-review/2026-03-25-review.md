# Code Review - YD YouTube Downloader

**Date:** 2026-03-25
**Scope:** Security, UI/UX, Accessibility
**Status:** Fixed

---

## Security Issues

### Critical

| # | Issue | Location | Status |
|---|-------|----------|--------|
| 1 | CSP disabled (`null`) - vulnerable to XSS | `tauri.conf.json` | Fixed |
| 2 | No checksum verification for yt-dlp in CI build | `release.yml` | Fixed |
| 3 | HTTP URLs accepted (insecure transport) | `download.rs`, `UrlInput.tsx` | Fixed |

### High

| # | Issue | Location | Status |
|---|-------|----------|--------|
| 4 | Unsafe FFI `i32` cast in `decode_output` - integer truncation risk | `download.rs` | Fixed |
| 5 | `taskkill` uses `.spawn()` (fire-and-forget) - no error checking | `download.rs` | Fixed |

### Medium

| # | Issue | Location | Status |
|---|-------|----------|--------|
| 6 | Overly broad `$HOME/**` file path permission | `capabilities/default.json` | Fixed |
| 7 | Temp file leak in `update_ytdlp` on error | `download.rs` | Fixed |
| 8 | URL length not bounded (potential abuse) | `download.rs` | Fixed |

### Fixed (Round 2 — 2026-03-26)

| # | Issue | Location | Status |
|---|-------|----------|--------|
| 11 | CSP missing `connect-src` for Tauri IPC | `tauri.conf.json` | Fixed |
| 12 | CI checksum TOCTOU — binary/checksums from different releases | `release.yml` | Fixed |
| 13 | ffmpeg has no checksum verification in CI | `release.yml` | Fixed |
| 14 | `taskkill` error on already-exited process breaks cancel | `download.rs` | Fixed |
| 15 | Common yt-dlp errors not translated | `i18n.tsx`, `DownloadItem.tsx` | Fixed |

### Not Fixed (Low Risk / Needs Discussion)

| # | Issue | Reason |
|---|-------|--------|
| 9 | ffmpeg metadata title not sanitized | ffmpeg treats `-metadata` value as literal; injection not exploitable since it's passed as a single arg, not shell-expanded |
| 10 | `.expect()` calls for app paths | These paths are guaranteed by Tauri runtime; converting to `Result` would add complexity for an impossible failure case |

---

## UI/UX Issues

### Fixed

| # | Issue | Location |
|---|-------|----------|
| 1 | Missing `role="alert"` on binary check banner | `App.tsx` |
| 2 | Update dismiss button (✕) has no accessible label | `App.tsx` |
| 3 | Search form missing `role="search"` | `SearchBar.tsx` |
| 4 | Header settings/back button missing `aria-label` | `App.tsx` |

### Not Fixed (Would Require Significant Refactoring)

| # | Issue | Recommendation |
|---|-------|----------------|
| 5 | `window.confirm()` for disk space warning | Replace with custom modal component in future |
| 6 | No success toast after settings save | Add toast/notification system |
| 7 | No loading skeleton on initial config load | Minor - config loads fast |
| 8 | No virtualization for large download lists | Consider `react-window` if lists exceed ~100 items |

---

## Accessibility Issues

### Fixed

| # | Issue | Location |
|---|-------|----------|
| 1 | PlaylistModal missing `role="dialog"`, `aria-modal`, `aria-labelledby` | `PlaylistModal.tsx` |
| 2 | PlaylistModal close button missing `aria-label` | `PlaylistModal.tsx` |
| 3 | PlaylistModal missing Escape key handler | `PlaylistModal.tsx` |
| 4 | Cancel/OpenFolder/Retry buttons missing `aria-label` | `DownloadItem.tsx` |
| 5 | Status icons (check, warning, X) missing `aria-label` | `DownloadItem.tsx` |
| 6 | Progress bar missing `role="progressbar"` and `aria-value*` | `DownloadItem.tsx` |
| 7 | No `:focus-visible` styles for keyboard navigation (scoped to avoid Tailwind conflicts) | `App.css` |
| 8 | Empty state SVG missing `aria-hidden` | `DownloadList.tsx` |
| 9 | URL textarea missing `aria-label` | `UrlInput.tsx` |
| 10 | Paste button missing `aria-label` | `UrlInput.tsx` |

### Fixed (Round 2 — 2026-03-26)

| # | Issue | Location |
|---|-------|----------|
| 11 | PlaylistModal has no focus trap | `PlaylistModal.tsx` |
| 13 | URL/Search tabs missing `role="tab"` / `aria-selected` | `App.tsx` |

### Not Fixed

| # | Issue | Recommendation |
|---|-------|----------------|
| 12 | Settings toggle buttons lack `aria-pressed` | Add in future accessibility pass |

---

## Code Quality Notes

### Positive

- Strong component separation with clear responsibilities
- Good memoization and event cleanup patterns
- Proper TypeScript strict mode
- Good i18n system with full Hebrew/English support
- RTL support works correctly
- Semaphore-based download rate limiting
- SHA256 verification for yt-dlp runtime updates
- rustls-tls instead of OpenSSL
- Input validation allowlists in `config.rs`

### Recommendations for Future

1. Add error boundaries for component crash recovery
2. Consider structured logging instead of `console.error`/`eprintln!`
3. Document lock ordering for `DownloadState` mutexes
4. Add `SIGKILL` fallback on Unix if `SIGTERM` doesn't terminate within timeout
